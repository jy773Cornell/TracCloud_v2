import {history} from './history'
import {
    setToken,
    getToken,
    removeToken
} from './token'
import {
    getCookie
} from "./get_cookie";

export {
    history,
    setToken,
    getToken,
    removeToken,
    getCookie
}
